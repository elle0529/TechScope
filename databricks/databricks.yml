bundle:
  name: techscope

include:
  - resources/*.yml

variables:
  existing_cluster_id:
    description: Existing Databricks cluster ID used by the one-day PoC job.
  bronze_base:
    description: ADLS Bronze root.
  silver_base:
    description: ADLS Silver root.
  gold_base:
    description: ADLS Gold root.
  rag_base:
    description: ADLS RAG root.

targets:
  dev:
    mode: development
    default: true
